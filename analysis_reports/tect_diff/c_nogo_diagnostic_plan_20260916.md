# C 固定最终检查点：NO_GO 后有界诊断登记

2026-09-16，登记在本次新诊断结果产生之前。C 已正常完成十轮并为 NO_GO，未执行完整两库确认或 Full/All8。既有 best epoch4、last/pilot_final epoch9、冻结来源及全部成绩保持原样。

## 对象与问题

- 唯一模型：`TECT-PILOT-CASIA2-GN8-N8192-R512-S42-20260916-C` 最终 epoch9 / step6830 `last.pth`，SHA256 `fdd3f519b91862f79b0268138666bc70abf1b1bc49163e2d0843889096f21390`。
- 执行原模型来源 `02691917da17016f64f26ef820d5ccfa175e18ab`，配置hash `c42769f4f4fedc18f5ae21ae4c6525bb551a416e2e5ffd98ed067bc6877bd702`。不推理 best，不重新训练，不重跑已完成的 B 诊断。
- 使用 B 诊断已固定的16个 TRAIN ID（8Au/8Tp，均已包含在C清单），逐项验证 C/B 行一致及与测试/probe 的 ID、RGB 和已登记 source 隔离。仅训练图进入模型；512网格诊断不等同于原尺寸 Test2 分数。
- 问题：C 的低召回是否也出现在这些训练图；偏差首先出现在图像条件预测、后续带噪掩码还是历史预测；真实/篡改图各自的损失分量和全局前景概率偏移方向是什么。所有诊断均不能单独证明跨库退化根因。

## 固定前向预算

双GPU，每rank固定8图，BF16、eval、原稳定ID噪声seed0、参考/校准保持。最多900秒、无优化器、反向传播或权重保存。

1. 原 `sample()` 十步一次，捕获每步 base/ctrl 输出、首末步原输入及参考证据。
2. 固定 mask 噪声，GT 加噪 t=.2/.5/.8/.95，各无历史一次。
3. 原首步 t=1、无历史、gamma=0，固定roll1错配成对RGB/HF PVT特征一次，其他路径/噪声不变。
4. 原末步同一时间，带噪掩码={真实轨迹输入,固定噪声GT加噪输入} × 历史={真实轨迹历史,无历史}。复用已有真实/真实组合，只增加3次前向。

合计每rank恰好18次task、8次参考前向。记录调用计数、每图混淆矩阵、预测/GT面积、前景/背景内概率、WBCE/WIoU、软前景总量及对统一logit偏移的解析导数。低噪声GT输入本来含标签信息，其高分不能单独证明捷径；移除历史或改输入若改善仅表示局部敏感性，不能改用该路径作正式结果。

## 无新增推理的已有结果分析

读取C既有十轮逐图标量结果，跟踪相同ID的完全漏检与预测面积；按原GT面积比例预先固定区间 `<1%`、`[1%,5%)`、`[5%,20%)`、`>=20%` 汇总。训练清单仅用原生mask元数据描述面积分布。不得按分数选择新的诊断样本、恢复概率曲线或重复Test2推理。

## 保全与解释边界

项目内独立临时目录、双GPU锁、C运行锁和诊断锁，确认原controller退出及GPU空闲后才运行。前后核验来源、终点文件、参数/缓冲区、参考/校准和RNG；记录进程身份及释放资源。保留小型收据和报告，临时脚本执行后移除，数据/权重留服务器。

C仍为 `selection_protocol=test_selected` 的开发 NO_GO。诊断不会改变门槛、阈值、终点、旧结果或自动启动另一训练；后续修订须据证据单独登记。完整训练的前三轮健康监督尚未完成，定时任务继续承担有界诊断/修订监督。

## 执行前元数据修订 B

首次诊断尝试于2026-09-16 01:19:43 UTC在清单检查阶段失败，尚未创建前向输出目录、导入torch或加载模型，task/reference前向均为0；监督器保留失败记录并释放资源。失败原因是原B固定样本与C重新生成的工程训练probe之间出现两对**已知原图来源交集**，不是测试或真实图probe交集。

- 固定诊断图 `CASIA2:Tp_D_NRN_S_N_nat10158_ani00058_12027`。
- 固定诊断图 `CASIA2:Tp_S_CNN_S_N_arc00011_arc00011_01110`。
- 二者各自与工程训练图 `CASIA2:Tp_D_NNN_L_N_arc00011_ani00058_00231` 共享已登记source；没有相同ID或相同RGB。

工程probe属于训练清单内的可逆检查，其八次更新及RNG在正式训练前已恢复，不是独立效果评估人口。为保持原16张诊断图，修订仅允许上述两对精确source交集，并在收据中披露；仍拒绝工程probe ID/RGB重叠及其他source交集，全部quick/full Test2和64真实图probe的ID/RGB/source隔离要求不变。这个修订发生在任何新模型输出产生前，不改变训练、测试、门槛或样本选取。

修订诊断使用独立目录 `runtime/tect-c-nogo-diagnostic-20260916-b` 和任务标识 `TECT-C-NOGO-DIAG-20260916-B`。初次失败目录和收据保留，不能覆盖。模型仍是同一个固定C最终epoch9，前向预算仍18task/8reference每rank，总预算900秒。
