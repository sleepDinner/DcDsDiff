# REFNORM-V2 完整重启与前期监督

## 本次授权与固定运行

2026-09-15，用户在一阶段修复完成后要求重新开始完整 TECT 实验，并加入定时任务监督前期训练是否正常；出现问题则停止并修改，不要求全程人工或模型监督。

- 新 RUN_ID：`TECT-DIFF-FULL-R512-S42-REFNORM-V2-20260915-B`。
- 配置：`full_r512_s42_refnorm_v2.json`；协议：`TECT-DIFF-FULL-R512-S42-REFNORM-V2`。
- 固定 seed42，512 输入，GPU0+GPU1 双 rank DDP。参考网络全局 batch16、20 轮（epoch0–19）；随后训练集校准；主模型全局 batch8、100 轮（epoch0–99）。
- 参考网络重新初始化，主干仅用登记的 ImageNet 权重。旧 A 的 epoch15 checkpoint、冻结源码、hold 和历史结果保留；不恢复 A、不采用早期 checkpoint、不把有界验证权重用于正式初始化。
- 复用经校验的数据审计缓存并由正式入口核验冻结 manifest。数据源保持只读；新运行的源码、配置、环境、manifest 和初始化权重哈希由控制器登记。
- 服务器脱离 SSH 的控制器自动执行 preflight → reference → calibration → main → reports → GitHub 白名单发布。全部科学设置沿用 REFNORM-V2；本次只新增运行身份和操作监督约定。

## 两层监督及终止范围

1. 训练代码在每次更新检查有限 loss / 梯度；参考阶段按已登记的 `REFERENCE-NOISE-HEALTH-v1` 检查每尺度 MSE、零预测 MSE、预测能量和噪声相关性。已学习后连续两轮全尺度退化会保留完整末轮状态并失败停止。固定 epoch19 及实际最终权重的训练图探针均须通过，才可校准及主训练。
2. 本 Codex 任务每 30 分钟执行一次有界 SSH 检查，读取小型状态、每轮参考健康指标和主训练诊断窗口。无需保持本地 SSH/sleep 循环，也不重新加载模型或追加推理来做例行检查。

前期监督覆盖参考网络全部 20 轮（包括旧版约 4200 次更新的退化位置）、校准衔接和主模型最初三轮 epoch0–2。确认以上均正常后写前期检查结论并将定时任务设为 PAUSED；之后完整训练继续由服务器控制器推进，训练内异常检查仍然有效。若到第三轮仍有待排查问题，先解决或记录明确失败，不能把不确定状态写成通过。

## 定时检查证据

- 先核对新运行 ID、配置哈希、源码提交、controller/worker PID 身份及两卡占用。正常运行不得重复启动；原 A 永远保持停止。
- 查看 `controller_status.json`、`progress_rank0.json`、`progress_rank1.json`、`reference_health.json`、最近参考指标和对应阶段日志尾部。短暂 SSH 失败只记录传输问题，不能当成训练失败而杀进程。
- 参考阶段重点看四尺度 `MSE/zero_MSE` 是否学到有效噪声预测、预测能量/相关性是否持续退化，以及固定最终权重检查是否通过。初始化的高误差和单个 minibatch 波动不构成退化证据。
- 主训练检查两 rank 的 `training_metrics.rank*.jsonl`：loss 分量、梯度、更新步数、AMP 跳步、q、gamma、校正幅度等实际已有字段。比较多个诊断窗口，并考虑随机时间/尺度前缀和真假图混合导致的波动。校准收据非空、参考路径冻结、主模型诊断及已有逐轮 All8 的覆盖与有限性也须通过。
- 停滞必须结合阶段、两个 rank 时间戳、日志推进、GPU 活动和已有阶段耗时确认。评测/保存期间没有优化器步数增长并非训练卡死。
- 有限但较低的 test-selected All8 F1 本身不触发调参或重训，也不能仅凭 loss 下降宣称定位收敛或创新有效。前期结论区分运行正常、存在学习信号和最终科学成绩。

## 异常处置授权

用户已授权对新运行确认的非有限数、已登记参考退化、持续卡死、冻结路径破坏或明确实现故障停止并修改。优先由现有训练保护失败停止；必要时调用本项目控制器的 `stop`，只操作经过 PID/start_ticks/命令行归属验证的该运行。保留最新完整 checkpoint、失败指标、日志、源码与配置；停止后确认子进程退出和锁释放。

修复应定位具体原因，采用最小工程改动，在项目和专用环境范围内完成有界验证、版本化记录、提交和发布。运输/控制器问题可在同一冻结训练协议下精确恢复；涉及训练源码或网络语义变化时不能修改旧 snapshot 或冒称 exact resume，须记录新的修复身份及继承/重新初始化边界。不得无界重试、增添 seed/消融/正式训练预算、事后选 checkpoint 或用测试表现搜索超参数。不能在原科学范围内解决的问题应保持停止并报告具体原因。

每次检查保存小型本地/服务器检查记录；状态未变或无须处置时保持安静。只在异常、修复、需要用户处理或前期监督完成时通知。前期通过后自动暂停本定时任务，不停止健康训练。

## 结果口径

`selection_protocol=test_selected`：主训练每轮八测试集参与 best 选择，不是独立泛化评估。固定 final 为 epoch99，best 按完整有限八集 macro per-image Pixel-F1 严格改善选取，平局留早轮。新训练启动和前期正常不代表完成全量训练或验证创新收益；无同协议完整 baseline 时不做该比较。
