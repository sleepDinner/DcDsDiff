# Progress

2026-09-14: Started authorized rebuild. Verified local Git state and SSH access. Delegated independent paper protocol, code fidelity and read-only server resource audits. Reviewed Git workflow, file planning and code review skills. Pending server and source evidence before fixing scientific settings.

2026-09-14: User accepted reconstructed-split assumptions explicitly. Registered GIT10K-PAPER-RECON-V1, one fresh main model, 100 epochs, final99 only. Implemented paper parameters/SA/CA, WBCE repair, paired horizontal flip, trace parity, single-process training, complete checkpoint/recovery and immutable source controller. Removed obsolete active files into ignored recoverable archive after automatic review rejected a bulk deletion command. New environment installation uses only requested prefix and project cache; reused verified official Torch wheel because download was slow. Controller real subprocess success/failure/interruption/grandchild and lock tests passed using new environment Python. Model runtime tests still pending full dependency readiness. Copied and verified original PVT-B2 initialization in new project.

2026-09-14: New environment reached verified JSON READY. CPU exact epoch-boundary resume passed with 0 and 4 workers. Environment guard detected unrelated user-site packages; fixed CLI isolation with Python -s/PYTHONNOUSERSITE and reran CPU checks in isolation. First full data candidate was rejected before training due to 3 cross-split duplicate RGB groups (26 duplicate pairs total). Updated split to bind identical RGB content groups while preserving 9000/1000 and seed42; regrouping recomputes every auxiliary pixel using pinned packages, reusing an old auxiliary file only when exactly equal. First candidate preserved as rejected evidence; no model was trained on it.

2026-09-14: All engineering gates passed: isolated environment, exact CPU epoch-boundary recovery with 0/4 workers, controller failure/orphan/interruption cleanup, real batch6 FP32 GPU forward/backward, 100% pretrained PVT feature coverage and actual two-image final-evaluator CLI. GPU probe is not a scientific result. Formal data READY with cross-split identical RGB=0; all 20000 reused d/t files were exactly pixel-equal to regeneration in pinned OpenCV4.9. Canonical LF manifest excludes cache-use bookkeeping so fresh/reused generation hashes agree; current manifest SHA d865abb3144ae843ba44a9d9d8e3bef547acdccaaad5936a64bd82e99b72d189. Publishing and formal startup remain.
