# Project rules

## Operational registration, 2026-09-16

- The next registered repair candidate is `TECT-PILOT-CASIA2-GN8-EMPTYBCE-R512-S42-20260916-D`, protocol `TECT-PILOT-CASIA2-GN8-R512-S42-DATA2-N8192-EMPTYBCE-V1`, config `configs/tect_diff/pilot_casia2_gn8_data2_n8192_emptybce_r512_s42.json`. See `configs/tect_diff/PILOT_EMPTY_TARGET_BCE.md`. Relative to C, change only protocol ID and `training.mask_loss=empty_target_bce_v1`: exactly zero targets on the actual512 training grid contribute original WBCE only; nonempty targets retain original WBCE+WIoU, with the same whole-batch mean and Image MSE. This is a testable objective revision, not a confirmed repair. All nine C manifests, GN8 architecture, fitting dependencies, seed42/ImageNet fresh initialization, micro6/eval8 settings and original ten-epoch/four-hour gates remain. Do not resume C, modify old snapshots, or alter thresholds/gates. D must pass source/data binding and reversible dual-GPU engineering checks before formal pilot training.
- C completed all ten epochs (final epoch9/step6830) at 2026-09-16 00:20:55 UTC as `COMPLETED / NO_GO`; reports published at `9ab9e02241a3dac630806ded3cdf162feaed10dd`, resources released. Best Test2 .507495 at epoch4 failed authentic FP requirements; final Test2 .235143. No complete two-set confirmation or Full training was performed. Preserve C terminal/source/checkpoints. The fixed-C-final diagnosis is now completed and recorded in `analysis_reports/tect_diff/c_nogo_diagnostic_20260916.md`; it supports the separately registered D loss candidate above, not another automatic data/epoch expansion or B diagnostic replay.
- Under the existing authorized CASIA2 stabilization scope, the next development revision is `TECT-PILOT-CASIA2-GN8-N8192-R512-S42-20260916-C`, protocol `TECT-PILOT-CASIA2-GN8-R512-S42-DATA2-N8192-V1`, config `configs/tect_diff/pilot_casia2_gn8_data2_n8192_r512_s42.json`. See `configs/tect_diff/PILOT_N8192.md`. B completed all ten registered epochs as NO_GO; preserve it and do not resume. Final-epoch9/16-training-image diagnosis showed useful prior-start localization and strong direct PVT conditioning, without a demonstrated sampling defect. This is not a proven cause or guaranteed remedy for the failed Test2 gate.
- C changes only the registered training pool to 4096 Au + 4096 Tp (8192 total), nested over B's 2048 unchanged rows. Keep B's quick/full Test2, authentic probe and fitting manifests/hashes. C is fresh seed42/ImageNet MAIN with the same GN8 architecture, uniform-t sampling, mechanisms, optimizer, batch, precision and evaluation rules. Maximum ten epochs/4h, at most6830updates; the old effectiveness/confirmation gates remain fixed. Coverage and update count change together, so improvement would not isolate their individual effects. This remains CASIA2 development, not READY_FOR_FULL or authorized premature FinalTrainData/All8 expansion.

## Latest user authorization, 2026-09-16

- Active pilot revision is `TECT-PILOT-CASIA2-GN8-R512-S42-20260916-B`, protocol `TECT-PILOT-CASIA2-GN8-R512-S42-DATA2-V1`, config `configs/tect_diff/pilot_casia2_gn8_data2_r512_s42.json`. Pilot A failed during data preparation before GPU/model initialization: two LA masks have luminance/alpha disagreement under the unchanged channel-consensus decoder. B explicitly quarantines only those two hash-bound image/mask pairs; all other semantics failures still block. See `configs/tect_diff/PILOT_DATA2_REPAIR.md`. A stays FAILED/HOLD with no trained checkpoint; B keeps the same GN8 architecture, sample budgets and effectiveness gates.

- The user explicitly authorizes framework and network-architecture changes needed to repair problems discovered by supervision in this project, including the proposed task-network GroupNorm8 repair. Do not ask again for that repair scope. Version each change, preserve old snapshots/checkpoints/results, and validate the repaired behavior; this authorization supersedes the earlier normalization-proposal approval hold below. A/B/C/D/E remain historical held runs; new repaired models receive new identities.
- While the model is unstable, use `/data1/data/datasets/CASIA2.0/` for training and only Casiav1 (`/data0/zh/VM-UNet/data/isic17/CAISA 1/`) and Columbia (`/data0/zh/VM-UNet/data/isic17/Columbia/`) for tests. The user permits smaller training/test subsets when more efficient for model validation. Fix manifests and success/expansion gates before observing new results. Do not spend a full FinalTrainData/All8 run until clear effectiveness is demonstrated. This explicitly revises the prior no-validation/no-extra-development-run limits for the authorized stabilization workflow, without resuming old runs or changing their results.
- Per-epoch results should follow the structure of `/data1/hl/DINOv3/output/SG-BB-HFNATIVE-SPATIAL-S42-20260914_005031/metrics_per_epoch.jsonl`: epoch_index/epoch_number/total_epochs, metric, pixel_f1 by dataset, image_counts and explicit selection metadata. Use `average_test2` for the two-set pilot; never label it All8. Keep `selection_protocol=test_selected` and distinguish subset development, complete two-set confirmation and later full-data training.
- The initial GN8 stabilization pilot is documented in `configs/tect_diff/PILOT_CASIA2_GN8.md`. It retains the verified frozen reference/calibration as explicit inherited fitting dependencies, while the main model is fresh seed42/ImageNet. These dependencies were fitted on FinalTrainData, so this is not a claim that every learned component was trained only on CASIA2. All newly named data roots are read-only; synthesized zero masks for explicitly identified CASIA2 Au images live only in this project.

## TECT-Diff authorization, 2026-09-15

- Operational update 2026-09-16: E was stopped under the existing abnormality authorization at 2026-09-15 18:22:48 UTC after complete main epochs0/1 showed persistent near-total foreground predictions. Preserve E last epoch1/step8102, best0 and frozen source; A/B/C/D/E remain held. The bounded16-training-image checkpoint diagnostic changed no weights/buffers/RNG and found strong BN-statistics sensitivity, not a proven deviation from the frozen normalization protocol. See `analysis_reports/tect_diff/main_foreground_diagnostic_20260916.md`. The subsequent user authorization above permits the registered fresh GN8 stabilization pilot; E itself remains held. The early healthy endpoint has not been reached.

- Latest same-session user revision explicitly stops healthy partial-main D and authorizes retuning and fresh MAIN restart, including both training and every-epoch All8 speed optimization. `TECT-DIFF-FULL-R512-S42-REFNORM-V2-MEMB6-20260915-E` uses `full_r512_s42_refnorm_v2_memb6.json`: micro6/rank, accumulation1, global12, LR1e-4; evaluation micro8/rank, metric_workers4/rank, queue8. This latest explicit micro6 request changes global batch from8 to12. Measured training-image validation and BF16_BATCH_EXECUTION_V1 disclose numerical batching differences; reference/loss/All8 mathematics remain fixed. See `configs/tect_diff/MEMORY_BATCH_RESTART.md`. Reuse D's hash-bound C reference epoch19/calibration unchanged, initialize MAIN fresh seed42/ImageNet for epochs0–99; no D main resume, reference retraining, calibration refit or extra scientific arm. A/B/C/D remain held. Preserve AMP/DDP guards and all other scientific/All8 rules. The existing 30-minute monitor follows the registered E through complete healthy main epochs0–2 and their All8, then pauses itself.

- The authorized early monitor confirmed an AMP/no_grad cache and DDP accumulation fault in C's partial main epoch0. C is held and has no complete main checkpoint. `TECT-DIFF-FULL-R512-S42-REFNORM-V2-AMPFIX-20260915-D` reuses C's completed, hash-bound reference epoch19 and training-fitted calibration, then initializes corrected MAIN fresh at seed42 for epochs0–99. No reference retraining, calibration refit, extra arm, or C main-state resume. See `configs/tect_diff/AMP_DDP_REPAIR.md`. The same 30-minute early monitor follows D through complete healthy main epochs0–2; A/B/C remain stopped. The new main guards require full registered gradient coverage, per-update rank norm/count agreement and parameter hash agreement before checkpoints.

- Same-session user extension requests measured server-resource optimization. `TECT-DIFF-FULL-R512-S42-REFNORM-V2-PERF-20260915-C` continues B's complete reference epoch1 / step1496 with identical configuration, model/optimizer/scheduler/scaler/all-rank RNG/sampler/history. Only audited synchronization/statistics scheduling changes; this is an explicit source-version continuation, not identical-source exact resume or an extra scientific arm. B is held after transfer; original A stays held. See `configs/tect_diff/PERFORMANCE_CONTINUATION.md`. The existing early-supervision task follows C and still ends after healthy main epochs0–2.

- Latest user request on 2026-09-15 authorizes a fresh complete REFNORM-V2 run after the repair: `TECT-DIFF-FULL-R512-S42-REFNORM-V2-20260915-B`, governed by `configs/tect_diff/full_r512_s42_refnorm_v2.json` and `configs/tect_diff/RESTART_V2.md`. Start the reference fresh, then execute training-only calibration and all 100 main epochs. A 30-minute task checks the reference stage and first three complete main epochs, then pauses itself after recording healthy early training. The user authorizes stopping this new run on a confirmed abnormality and making bounded, versioned engineering repairs; preserve the frozen snapshot, checkpoints, scientific settings, and run provenance. This supersedes the earlier repair-task-only restriction for the new run; original A remains stopped.

- Later user request on 2026-09-15 stops the degenerated Stage A reference in `TECT-DIFF-FULL-R512-S42-20260915-A`. Keep that run stopped and preserve its epoch15 checkpoint/frozen snapshot. The versioned reference repair is described in `configs/tect_diff/REFNORM_V2.md`; this repair task performs bounded engineering validation only and does not restart formal training or select an earlier reference checkpoint.

- TECT-Diff is a distinct user-authorized protocol, governed by `configs/tect_diff/full_r512_s42.json` and `configs/tect_diff/PROTOCOL.md`. Its dual-GPU DDP, global batch 8, 512-first input, FinalTrainData training and All8 macro per-image Pixel-F1 selection replace the historical single-GPU/batch6/final99-only rules **only for TECT**. Never resume the stopped CASIA parent/continuation or rerun completed original evaluations.
- Authorized scope: one seed42 FULL main run, 20 fixed-epoch reference training if needed, training-only calibration, 100 main epochs and automatic reports. No validation split, extra seed, baseline or ablation run. Whole valid training manifest remains in main training despite reference/calibration roles.
- All `/data0/hl/FinalTrainData` and eight registered benchmark roots are read-only. Confirm pair mappings, authenticity and zero-mask semantics; isolate invalid pairs/exact leakage by manifests, never modify source data. No missing-mask authenticity inference or test-derived reference/statistics/hyperparameters.
- New code and all operational files stay in this project; only necessary small environment fixes in `/data0/hl/conda_envs/dcdsdiff` are authorized. Redirect cache/temp variables and bytecode into the project; no unrelated environment/process/data edits.
- FULL requires a genuinely trained frozen reference and nonempty training-fitted calibration. Preserve independent Image noises, shared noise across scales, protected reference path, signed cross-replica trajectories, real Image MSE, bounded evidence correction and x0-consistent Mask DDIM. Never silently disable mechanisms to fit memory.
- Both GPU locks and run lock are required; respect inherited legacy locks, inspect occupancy and never terminate unrelated processes. Run a published fixed source snapshot with torchrun two ranks. Preserve exact resume model/optimizer/scheduler/scaler/all-rank RNG/sampler/epoch and pending evaluation state.
- Every main epoch evaluates all eight sets once per image with stable ID-derived noise, no DDP padding, last-step P_ctrl, sigmoid before original-size bilinear restore, fixed threshold 0.5. Only complete finite eight-set results can atomically replace best.pth; strict improvement wins, ties retain earlier epoch. Save independently reconstructible last/best/final; final is fixed epoch99.
- Every report must say `selection_protocol=test_selected`: eight test sets participate in checkpoint selection and are not independent generalization evaluation. No comparison to a full same-protocol baseline unless such a run exists.
- Every run automatically updates status/ledger/report at completion, failure, termination or recovery. Server controller performs A -> B -> C -> reports -> whitelist GitHub publication. Publication uses a separate project worktree and never mutates running snapshots; no force push. Report incomplete and pending_sync honestly.
- Failed-start cleanup requires realpath, run ownership and task marker verification. Retain valid trained checkpoints, reference/calibration artifacts and historical results. Never apply recursive project-root cleanup or traverse links into datasets.

- This checkout reproduces DcDsDiff and is the base for future controlled innovations. Read README.md, docs/paper_protocol_audit.md, docs/reproduction_protocol.md and experiment_ledger.md before changing a scientific path.
- Baseline `GIT10K-PAPER-RECON-V1` is a documented reconstruction, not an exact official split. Never silently relabel filename prefixes as paper generator categories, reuse historical trained weights as fresh initialization, or claim a running job has reproduced paper scores.
- Keep the baseline fixed. A new idea gets its own named config/protocol ID, Git commit and run ID. Do not change a running source snapshot or frozen data manifest. Additional formal runs need user authorization.
- Work only in this project, `/data1/hl/DcDsDiff-and-GIT10K`, and its environment `/data0/hl/conda_envs/dcdsdiff`. Original GIT10K and all CASIA2/All8 roots in `config/benchmark_all8.json` are read-only data sources. Read/copy useful old project resources; do not modify other environments, processes or datasets.
- Never overwrite CUDA_VISIBLE_DEVICES in imported code. Each controller is single GPU/single process, global batch 6. New runs hold both per-GPU and per-run locks; also respect the original controller's inherited global lock and GPU. Check occupancy before launching and keep resume bound to its registered GPU. Never stop an unrelated process.
- Save full optimizer/scheduler/scaler/RNG state and next_epoch; resume only the same frozen protocol at an epoch boundary. Old checkpoints without those fields are not exact resumptions.
- Primary result is predeclared final epoch 99, inference seed 0, 10 steps, threshold 0.5. Best MAE is test-selected diagnostic only. No post-hoc checkpoint or threshold sweep.
- User extension on 2026-09-14: one CASIA2-ALL8-V1 training arm on GPU 1 changes data only; its requested eight-set report uses the existing `model-best.pt` selected by pooled All8 MAE. Original GIT10K keeps final99 as its reproduction endpoint and receives one additional All8 evaluation of its GIT10K-MAE-selected `model-best.pt` after its controller completes. Do not create checkpoint aliases. See `docs/casia2_all8_protocol.md` for these distinct selection populations.
- Later user revision on 2026-09-14 supersedes the GPU 1 All8-per-epoch arm: stop A, import its last complete epoch-2 checkpoint into `CASIA2-SEL3-ALL8-V1`, and continue epochs 3–99 with Casiav1/Columbia/NIST16 only (1,664 images) for pooled-MAE best selection. Preserve optimization/RNG state, reset best at this explicit boundary, and label the inherited All8 history. Final reports for the CASIA2 continuation and original GPU 0 both use their existing best checkpoint on All8. Do not resume the superseded parent. See `docs/casia2_sel3_continuation.md`.
- User revision on 2026-09-15 ends GPU 1 B early after completed epochs 0–84 (85 total). Use its existing epoch-52 `model-best.pt` for one All8 evaluation under `CASIA2-SEL3-ALL8-EARLYSTOP-V1`; do not resume training or claim final99 completion. Preserve the original frozen training records and bind the early endpoint with `early_stop.json`. See `docs/casia2_early_stop.md`. GPU 0 already completed both registered evaluations; do not rerun it.
- Deep-model validation runs in the new server environment. Use bounded temporary validation scripts and remove them afterward; retain concise validation receipts, not test checkpoints or large outputs, in Git.
- Keep datasets, checkpoints, caches and local cleanup backups out of Git. Record source commit, environment, data/weight hashes and real run state in each run. Keep the ledger current.
- Supervision lives on the server: detached controllers, inherited resource locks and 60-second lightweight state updates. The original frozen controller retains final evaluation; its separate follow-up waits for completion before one All8 best evaluation. Do not use continuous local SSH/sleep loops.
- Before publication inspect the actual diff, run relevant bounded checks, and confirm local/GitHub/server commit agreement. Never force-push shared history.
